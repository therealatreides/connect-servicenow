# ==============================================================================
# ServiceNow Connector — Environment Configuration
# ==============================================================================
#
# This file configures ServiceNow instance connections for the connect-servicenow
# skill. When connected, the skill executes CRUD operations, aggregations, schema
# introspection, attachment management, batch operations, and health checks against
# live instances via scripts/sn.sh.
#
# Copy this file to `.env` and fill in your values.
#
# IMPORTANT: .env files contain credentials and MUST be listed in .gitignore.
# The skill will refuse to read .env if it is not in .gitignore (when inside a
# git repository). Exception: if not inside any git repository, the .gitignore
# requirement is waived.
#
# COMPATIBILITY: This .env format is shared across all ServiceNow skills
# (servicenow-developer, servicenow-story-intake, servicenow-troubleshooter, etc.)
# A single .env file works with all of them.
#
# FILE LOCATIONS (checked in order):
#   1. ./.env              — Project-level (preferred)
#   2. ~/.claude/.servicenow.env — Global fallback
#
# See references/env-file-format.md for full schema and validation rules.
# See references/authentication.md for auth strategy details.
#
# ==============================================================================


# ------------------------------------------------------------------------------
# DEFAULT INSTANCE (unprefixed SNOW_* variables)
# ------------------------------------------------------------------------------
# Used when no alias is specified or only one instance is configured.

SNOW_INSTANCE_URL=https://yourinstance.service-now.com
SNOW_AUTH_TYPE=basic

# --- Basic Auth (auth_type=basic) ---
SNOW_USERNAME=your_username_here
SNOW_PASSWORD=your_password_here

# --- OAuth ROPC (auth_type=oauth_ropc) ---
# Requires: client_id, client_secret, username, password
# SNOW_AUTH_TYPE=oauth_ropc
# SNOW_CLIENT_ID=your_client_id_here
# SNOW_CLIENT_SECRET=your_client_secret_here
# SNOW_USERNAME=your_username_here
# SNOW_PASSWORD=your_password_here

# --- OAuth Client Credentials (auth_type=oauth_client_credentials) ---
# Requires: client_id, client_secret (no user password needed)
# Available on ServiceNow Washington DC and later releases.
# SNOW_AUTH_TYPE=oauth_client_credentials
# SNOW_CLIENT_ID=your_client_id_here
# SNOW_CLIENT_SECRET=your_client_secret_here


# ------------------------------------------------------------------------------
# NAMED INSTANCE: DEV (prefixed SNOW_DEV_* variables)
# ------------------------------------------------------------------------------
# Access via alias "DEV" in the instance selection menu.

# SNOW_DEV_INSTANCE_URL=https://yourdev.service-now.com
# SNOW_DEV_AUTH_TYPE=basic
# SNOW_DEV_USERNAME=your_username_here
# SNOW_DEV_PASSWORD=your_password_here


# ------------------------------------------------------------------------------
# NAMED INSTANCE: TEST (prefixed SNOW_TEST_* variables)
# ------------------------------------------------------------------------------
# Access via alias "TEST" in the instance selection menu.

# SNOW_TEST_INSTANCE_URL=https://yourtest.service-now.com
# SNOW_TEST_AUTH_TYPE=oauth_ropc
# SNOW_TEST_CLIENT_ID=your_client_id_here
# SNOW_TEST_CLIENT_SECRET=your_client_secret_here
# SNOW_TEST_USERNAME=your_username_here
# SNOW_TEST_PASSWORD=your_password_here


# ------------------------------------------------------------------------------
# NAMED INSTANCE: PROD (prefixed SNOW_PROD_* variables)
# ------------------------------------------------------------------------------
# Access via alias "PROD" in the instance selection menu.
# Uses Client Credentials — no user password needed.

# SNOW_PROD_INSTANCE_URL=https://yourprod.service-now.com
# SNOW_PROD_AUTH_TYPE=oauth_client_credentials
# SNOW_PROD_CLIENT_ID=your_client_id_here
# SNOW_PROD_CLIENT_SECRET=your_client_secret_here


# ==============================================================================
# AUTH TYPE SUMMARY
# ==============================================================================
#
# basic                    — Username + Password (default, works on all releases)
# oauth_ropc               — Client ID/Secret + Username/Password (token-based)
# oauth_client_credentials — Client ID/Secret only (Washington DC+, no user password)
#
# ==============================================================================
