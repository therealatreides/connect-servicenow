# ServiceNow PDI Credentials for Integration Tests
# Copy this file to .env.test and fill in your values.
# This file is gitignored and should never be committed.
#
# IMPORTANT: Passwords and secrets use base64 encoding to avoid shell
# escaping issues with special characters ($, !, ^, etc.).
#
# Encode:  echo -n 'your-password-here' | base64
# Decode:  echo 'base64string' | base64 -d

# ── Default Profile (basic auth) ──────────────────────────────────────
SNOW_INSTANCE_URL=https://your-instance.service-now.com
SNOW_AUTH_TYPE=basic
SNOW_USERNAME=admin
SNOW_PASSWORD_B64=your-base64-encoded-password

# ── Additional Profile: OAuth ROPC ────────────────────────────────────
# Uncomment to test with OAuth Resource Owner Password Credentials.
# The profile name ("ROPC") can be anything; it becomes --profile ropc.
#
# SNOW_ROPC_INSTANCE_URL=https://your-instance.service-now.com
# SNOW_ROPC_AUTH_TYPE=oauth_ropc
# SNOW_ROPC_CLIENT_ID=your_client_id
# SNOW_ROPC_CLIENT_SECRET_B64=your-base64-encoded-client-secret
# SNOW_ROPC_USERNAME=admin
# SNOW_ROPC_PASSWORD_B64=your-base64-encoded-password

# ── Additional Profile: OAuth Client Credentials ──────────────────────
# Uncomment to test with OAuth Client Credentials grant.
#
# SNOW_PDI_INSTANCE_URL=https://your-instance.service-now.com
# SNOW_PDI_AUTH_TYPE=oauth_client_credentials
# SNOW_PDI_CLIENT_ID=your_client_id
# SNOW_PDI_CLIENT_SECRET_B64=your-base64-encoded-client-secret
